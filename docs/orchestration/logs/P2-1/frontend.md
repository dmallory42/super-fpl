# P2-1 Frontend Agent Log

- Status: done
- Notes:
- Added live data confidence strip on `Live` page with source badge (`real`/`estimated`), sample size, updated time, and stale indicator.
- Added provenance helper `getSampleProvenance` in `frontend/src/hooks/useLiveSamples.ts`.
- Added targeted browser test in `frontend/e2e/live.spec.ts`.
